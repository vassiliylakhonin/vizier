{
  "name": "vizier",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "description": "Deterministic authorization checks for agent actions.",
  "scripts": {
    "build": "npm run build:packages && wrangler deploy --dry-run --outdir dist",
    "build:packages": "npm run build --workspace @vizier/sdk && npm run build --workspace @vizier/gated-deploy && npm run build --workspace @vizier/mcp-proxy",
    "check": "npm run build --workspace @vizier/sdk && npm run typecheck && npm run check:migrations && npm run lint && npm test && npm run build",
    "check:deployed": "node scripts/check-deployed.mjs",
    "check:live": "node scripts/check-live.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "deploy": "node scripts/deploy.mjs",
    "deploy:gated": "npm run build:packages && node packages/gated-deploy/dist/cli.js",
    "dev": "wrangler dev --local",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "wrangler types --check && tsc -p tsconfig.json && tsc -p tsconfig.test.json && tsc -p packages/sdk/tsconfig.json --noEmit && tsc -p packages/gated-deploy/tsconfig.json --noEmit && tsc -p packages/mcp-proxy/tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "@eslint/js": "10.0.1",
    "@types/node": "26.2.0",
    "eslint": "10.8.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.66.0",
    "vitest": "4.1.10",
    "wrangler": "^4.128.0"
  },
  "engines": {
    "node": ">=24"
  }
}
